services:
  - type: web
    name: pettrackcare-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
    pythonVersion: 3.11.8
    healthCheckPath: /health